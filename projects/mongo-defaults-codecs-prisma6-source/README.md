# mongo-defaults-codecs-prisma6-source

Transient project folder. `spec.md` holds what is true at the project level, `design-notes.md` the decisions and rejected alternatives, `plan.md` the slice sequence. Slice specs and plans live under `slices/<NN-slug>/`. Deleted at close-out after long-lived docs move to `docs/`.
