# Slice C plan — delete `dbgenerated`

Spec: [`spec.md`](spec.md). Branch `remove-dbgenerated-delete` off `main` at `6f5fc8d81f` (2026-09-22, after slices A and B merged). One implementer (Fable) and one reviewer (Opus), resumed across every dispatch. Review artifact: `wip/reviews/code-review.md` (gitignored). Every dispatch: tests first and red before the change; commits staged explicitly and signed off; the tree typechecks and the dispatch's package tests are green at the end of each dispatch.

## Pre-slice health check (2026-09-22)

- Slices A and B are merged; `main` was green through the merge queue.
- Shared-file rebase: `scalarDefaultArms` in `contract-psl/src/sql-attribute-specs.ts` builds its arms from the data-type entries slice B introduced; the `taggedLiteral` arm from A is folded into that. No further reconciliation needed.
- The Supabase drift from `deferred.md` item 7 was regenerated on `main` in #30346. The committed `contract.prisma` now carries 15 `dbgenerated` lines, not 21: ten `gen_random_uuid()`, four `'{}'::jsonb` / `'[]'::jsonb`, one `(now() + '00:03:00'::interval)`. The six enum casts already print as string literals (`@default("STANDARD")`), so C4's enum bullet and C5's six enum rows are already done. The enum print form stays a string literal; the member-name form in C4 is superseded by the C5 note.
- `git grep` inventory of `dbgenerated` on `main` outside the Prisma 7 source and history: 41 files. It matches the spec's C1 to C8 surfaces plus these the spec does not name: `contract-psl/test/default-function-registry.test.ts`, `contract-psl/test/provider.test.ts`, `contract-psl/test/interpreter.defaults.tagged-literal.test.ts` (expected arm list), `9-family/src/core/migrations/contract-to-schema-ir.ts` (comments), `postgres/src/core/default-normalizer.ts` and its test (comments and test names), `postgres/test/migrations/schema-verify.namespaced-enum.integration.test.ts` (comment), `packages/3-extensions/postgres/README.md` (Prisma 7 diagnostics table; keep the Prisma 7 wording, drop the Prisma 8 suggestion), `test/integration/test/cli-journeys/infer-roundtrip-fidelity.prisma7-defaults.e2e.test.ts`, `test/integration/test/cli.db-introspect.e2e.test.ts`.

## Dispatches

| # | Dispatch | Outcome (true when it lands) | Builds on | Hands to | Gate |
|---|---|---|---|---|---|
| 1 | Prisma 7 source maps `dbgenerated` directly | Spec C3 in full: `defaults.ts` lowers `dbgenerated("x")` to a function default without the registry, the empty form to no default on every field with no diagnostic, other shapes to `PRISMA7_UNKNOWN_DEFAULT`; the required-field diagnostic is gone; new fixture for the empty form on a required field; package README and `packages/3-extensions/postgres/README.md` and `docs/reference/error-reference.md` Prisma 7 rows say so | spec | 2 | `contract-prisma7` tests; root typecheck; `pnpm lint` on the package; `pnpm fixtures:check` |
| 2 | `dbgenerated` is gone from Prisma 8 PSL | Spec C1 and C2: both adapter registries, the test fixture registry and the SQLite `NOW_SYNONYMS` rewrite deleted; the removed-function message; the `removed-dbgenerated` diagnostics fixture; the `invalid-default-arguments` fixture rewritten; every in-repo PSL input that wrote `dbgenerated(...)` rewritten to the form its column allows (C6, including the parity pair rename, the mapped-enums port fixture, the interpreter and provider tests, the adapter tests, the language-server completion tests, the `widening.test.ts` and SQLite normalizer comments); `git grep -n "NOW_SYNONYMS\|lowerDbgenerated\|dbgeneratedSig" -- packages` empty | 1 | 3 | `contract-psl`, both adapter, `language-server` tests; root typecheck; `pnpm test:packages`; `pnpm fixtures:check` (only the diagnostics fixtures and the renamed parity pair change); `pnpm test:integration` |
| 3 | `contract infer` prints the new forms | Spec C4: the family's `mapDefault` prints `` @default(sql`...`) `` (quote fence when the body has a backtick) for any function default that is not a named function; `fallbackFunctionAttribute`, `DefaultMappingResult.comment` and `formatDbGeneratedAttribute` deleted; `'<text>'::<column's own type>` classified as a literal read through the column's codec so a jsonb `'{}'` default prints the way slice B's printer writes a `json` value (`` @default(json`{}`) ``); infer round-trip journeys assert no `dbgenerated`; `git grep -n "formatDbGeneratedAttribute\|fallbackFunctionAttribute" -- packages` empty | 2 | 4 | `9-family` and `postgres` tests; root typecheck; `pnpm test:packages`; the `infer-roundtrip-fidelity*` and `cli.db-introspect` journeys |
| 4 | Supabase contract regenerated; old contract still loads | Spec C5 and C9: `contract.json` from `main` copied to `test/integration/test/fixtures/contract-format/supabase-before-dbgenerated-removal.contract.json` with a test that validates it; `pnpm --filter @internal/extension-supabase contract:generate` changes exactly the 15 `dbgenerated` lines of `contract.prisma` (halt otherwise); `CONTRACT-FIDELITY.md` column-defaults paragraph rewritten; Supabase package tests green including `reference-fixture-verify` | 3 | 5 | `@internal/extension-supabase` tests; `pnpm fixtures:check`; the new integration test; root typecheck |
| 5 | Docs, upgrade instruction, editor brief, PR | Spec C7, C8, C10: `upgrade-instructions/pending/remove-dbgenerated/{app,extension}/instructions.md`; every doc in C8 swept; ADR 167 dated note; scorecards; `projects/remove-dbgenerated/editor-tooling-brief.md`; the spec's Definition-of-done grep returns only the allowed files; every Definition-of-done command green; PR opened against `main` from the `bot` remote | 4 | PR | `pnpm typecheck`, `pnpm test:packages`, `pnpm test:integration`, `pnpm test:e2e`, `pnpm lint`, `pnpm lint:deps`, `pnpm lint:docs`, `pnpm lint:throws`, `pnpm fixtures:check`, `pnpm check:upgrade-coverage --mode pr`, `pnpm check:error-reference` |

## Decisions pinned for the implementer

- The enum-cast print form is a string literal (`@default("STANDARD")`), as `main` already prints it. Do not change it to the member name.
- The upgrade instruction's enum row therefore says `@default("<member>")` for the rewrite, matching what infer prints.
- Dispatch 2 rewrites every PSL input in the same dispatch as the registry deletion. *Amended after dispatch 2:* `pnpm fixtures:check` builds the Supabase contract space, whose `contract.prisma` still carries `dbgenerated` until dispatch 4 regenerates it, so that one gate is red from dispatch 2 until dispatch 4. Only the branch tip must be green; the gate moves to dispatch 4.
- Comments that only name `dbgenerated` as history (for example in `contract-to-schema-ir.ts`) are rewritten to describe the current behaviour, not deleted with the code around them.

## Open items

- Linear: the project has no ticket. The PR title carries no ticket prefix and the checklist says so, as slices A and B did.
- `deferred.md` item 8 (quote-aware SQL body check) is unchanged by this slice.
- `test/integration` `driver-adapters-error-forwarding`: an `it.fails` case passed during dispatch 2's run in two vitest projects at once. The reviewer read the test: it drives a mocked `pg` Pool through the ORM and nothing in this slice is on that path. Dispatch 5 re-runs it; if it still passes, it is checked on `main` and reported as a pre-existing item, not fixed here.
