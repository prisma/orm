datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model Post {
  id     String @id @default(auto()) @map("_id") @db.ObjectId
  count  Int    @db.Long
  title  String @db.String
  number Int    @db.ObjectId
}
