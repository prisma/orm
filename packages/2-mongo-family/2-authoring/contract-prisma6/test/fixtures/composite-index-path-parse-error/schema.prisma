datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model Post {
  id      String  @id @default(auto()) @map("_id") @db.ObjectId
  title   String
  address Address

  @@index([title, address.city])
  @@unique([address.geo.lat(sort: Desc)])
  @@fulltext([address.street])
}

type Address {
  street String
  city   String
  geo    Geo
}

type Geo {
  lat Float
  lng Float
}
