-- CreateSchema
CREATE SCHEMA IF NOT EXISTS "public";

-- CreateTable
CREATE TABLE "Decimals" (
    "id" INTEGER NOT NULL,
    "long" DECIMAL(65,30) NOT NULL DEFAULT 12345678901234567890.123456789,
    "tiny" DECIMAL(65,30) NOT NULL DEFAULT 0.000000000000000001,
    "negative" DECIMAL(65,30) NOT NULL DEFAULT -1.5,
    "whole" DECIMAL(65,30) NOT NULL DEFAULT 42,
    "zerosBare" DECIMAL NOT NULL DEFAULT 1.50,
    "zerosScaled" DECIMAL(10,2) NOT NULL DEFAULT 1.50,
    "zerosDefault" DECIMAL(65,30) NOT NULL DEFAULT 1.50,
    "list" DECIMAL(65,30)[] DEFAULT ARRAY[1.50, -2, 0.000000000000000001]::DECIMAL(65,30)[],
    "bigLong" BIGINT NOT NULL DEFAULT 9007199254740993,
    "bigList" BIGINT[] DEFAULT ARRAY[9007199254740993, -1]::BIGINT[],

    CONSTRAINT "Decimals_pkey" PRIMARY KEY ("id")
);
