datasource db {
  provider = "postgresql"
  schemas  = ["one", "two"]
}

model Post {
  id   Int   @id
  tags Tag[]

  @@schema("one")
}

model Tag {
  id    Int    @id
  posts Post[]

  @@schema("two")
}

model Tagging {
  id    Int    @id
  extra String

  @@map("_PostToTag")
  @@schema("two")
}
