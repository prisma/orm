// use prisma-8

// Member order is deliberately non-alphabetical: the round-trip must return
// the authored order, not a sorted one. The default must survive the round
// trip as the bare member identifier.
enum AccountsRole {
  user  = "user"
  admin = "admin"

  @@type("pg/text@1")
}

namespace public {
  native_enum AalLevel {
    aal1 = "aal1"
    aal2 = "aal2"
  }

  model Accounts {
    id   Int @id
    role AccountsRole @default(user)
    aal  pg.enum(AalLevel)?

    @@map("accounts")
  }
}
