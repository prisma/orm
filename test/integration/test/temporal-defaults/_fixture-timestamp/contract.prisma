model Stamp {
  id          Int    @id
  label       String
  updatedAt   temporal.timestamp(3, onCreate: now, onUpdate: now)
  updatedAtTz temporal.timestamptz(3, onCreate: now, onUpdate: now)
  @@map("stamp")
}
