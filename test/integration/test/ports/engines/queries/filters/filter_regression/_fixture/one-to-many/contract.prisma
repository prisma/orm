// use prisma-8

model Location {
  id        Int      @id
  name      String?
  companyId Int?
  company   Company? @relation(fields: [companyId], references: [id])
  @@map("location")
}

model Company {
  id        Int        @id
  name      String?
  locations Location[]
  @@map("company")
}
