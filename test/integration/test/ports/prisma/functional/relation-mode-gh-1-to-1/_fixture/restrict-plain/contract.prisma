model UserOneToOne {
  id              String @id
  profile         ProfileOneToOne?
  profileOptional ProfileOptionalOneToOne?
  enabled         Boolean?
  @@map("userOneToOne")
}

model ProfileOneToOne {
  id      String            @id
  user    UserOneToOne      @relation(fields: [userId], references: [id], onUpdate: Restrict, onDelete: Restrict)
  userId  String            @unique
  enabled Boolean?
  @@map("profileOneToOne")
}

model ProfileOptionalOneToOne {
  id      String            @id
  user    UserOneToOne?     @relation(fields: [userId], references: [id], onUpdate: Restrict, onDelete: Restrict)
  userId  String?           @unique
  enabled Boolean?
  @@map("profileOptionalOneToOne")
}
