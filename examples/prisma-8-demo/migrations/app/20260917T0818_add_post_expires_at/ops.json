[
  {
    "id": "column.public.post.expiresAt",
    "label": "Add column \"expiresAt\" to \"post\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "expiresAt",
        "table": "post"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"expiresAt\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": ["public", "post", "expiresAt"]
      }
    ],
    "execute": [
      {
        "description": "add column \"expiresAt\"",
        "sql": "ALTER TABLE \"public\".\"post\" ADD COLUMN \"expiresAt\" timestamptz DEFAULT ((now() + '7 days'::interval)) NOT NULL"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"expiresAt\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": ["public", "post", "expiresAt"]
      }
    ]
  }
]
